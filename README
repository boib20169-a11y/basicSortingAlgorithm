I built this command-line tool to step away from theoretical textbook definitions and actually see sorting algorithms work in the real world. 
Running directly in the terminal, it pitches Merge Sort against Insertion Sort to show exactly how dataset size and randomness change performance. 
It’s a practical exploration of when to use a heavy-duty algorithm versus a simpler one.
